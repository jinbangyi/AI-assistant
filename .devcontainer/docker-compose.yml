version: '3'

services:
  # Dify Services
  dify-api:
    image: langgenius/dify-api:latest
    container_name: dify-api
    ports:
      - "5001:5001"
    environment:
      - MODE=api
      - LOG_LEVEL=INFO
      - SECRET_KEY=${DIFY_SECRET_KEY:-sk-xxxxx}
      - DATABASE_URL=postgresql://postgres:postgres@dify-db:5432/dify
      - REDIS_HOST=dify-redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - REDIS_PASSWORD=
      - CELERY_BROKER_URL=redis://dify-redis:6379/1
      - WEB_API_CORS_ALLOW_ORIGINS=*
    volumes:
      - dify-storage:/app/api/storage
    depends_on:
      - dify-db
      - dify-redis
    networks:
      - devcontainer-network

  dify-web:
    image: langgenius/dify-web:latest
    container_name: dify-web
    ports:
      - "3000:3000"
    environment:
      - CONSOLE_API_URL=http://dify-api:5001
      - APP_API_URL=http://dify-api:5001
      - APP_WEB_URL=http://localhost:3000
    depends_on:
      - dify-api
    networks:
      - devcontainer-network

  dify-worker:
    image: langgenius/dify-api:latest
    container_name: dify-worker
    command: celery -A app.celery worker -P gevent -c 1 --loglevel INFO -Q dataset,generation,mail
    environment:
      - MODE=worker
      - LOG_LEVEL=INFO
      - SECRET_KEY=${DIFY_SECRET_KEY:-sk-xxxxx}
      - DATABASE_URL=postgresql://postgres:postgres@dify-db:5432/dify
      - REDIS_HOST=dify-redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - REDIS_PASSWORD=
      - CELERY_BROKER_URL=redis://dify-redis:6379/1
    volumes:
      - dify-storage:/app/api/storage
    depends_on:
      - dify-db
      - dify-redis
    networks:
      - devcontainer-network

  dify-db:
    image: postgres:15-alpine
    container_name: dify-db
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=dify
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - dify-db-data:/var/lib/postgresql/data
    networks:
      - devcontainer-network

  dify-redis:
    image: redis:7-alpine
    container_name: dify-redis
    # ports:
    #   - "6379:6379"
    volumes:
      - dify-redis-data:/data
    networks:
      - devcontainer-network

  # Open WebUI Service
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    ports:
      - "8080:8080"
    volumes:
      - open-webui-data:/app/backend/data
    environment:
      - WEBUI_SECRET_KEY=${WEBUI_SECRET_KEY:-secret-key-change-in-production}
      - OPENAI_API_BASE_URL=${OPENAI_API_BASE_URL:-}
      - DEFAULT_MODELS=${DEFAULT_MODELS:-gpt-3.5-turbo,gpt-4}
      - ENABLE_SIGNUP=true
      - ENABLE_COMMUNITY_SHARING=false
    restart: unless-stopped
    networks:
      - devcontainer-network

  # LiteLLM Database
  litellm-db:
    image: postgres:15-alpine
    container_name: litellm-db
    environment:
      - POSTGRES_USER=litellm
      - POSTGRES_PASSWORD=${LITELLM_DB_PASSWORD:-litellm_password}
      - POSTGRES_DB=litellm
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - litellm-db-data:/var/lib/postgresql/data
    networks:
      - devcontainer-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U litellm"]
      interval: 10s
      timeout: 5s
      retries: 5

  # LiteLLM Service
  litellm:
    image: ghcr.io/berriai/litellm:v1.80.0-stable.1
    container_name: litellm
    ports:
      - "4000:4000"
    environment:
      - DATABASE_URL=postgresql://litellm:${LITELLM_DB_PASSWORD:-litellm_password}@litellm-db:5432/litellm
      - REDIS_URL=redis://litellm-redis:6379
      - CONFIG_FILE_PATH=/app/litellm_config.yaml
      - USE_PRISMA_MIGRATE=true
      - UI=true
      - ADMIN_UI=true
      - LOG_REQUESTS=true
      - LITELLM_SALT_KEY=${LITELLM_SALT_KEY:-change-this-to-a-random-string}
      - LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY:-sk-nftgo-1234-abcd}
      - LITELLM_LOG=INFO
      - STORE_MODEL_IN_DB=true
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://signoz-otel-collector:4317
      - OTEL_EXPORTER_OTLP_HEADERS=signoz-ingestion-key=litellm
      - OTEL_EXPORTER_OTLP_PROTOCOL=grpc
      - OTEL_TRACES_EXPORTER=otlp
      - OTEL_METRICS_EXPORTER=otlp
      - OTEL_LOGS_EXPORTER=otlp
    volumes:
      - litellm-data:/app/litellm
      - ./configs/litellm_config.yaml:/app/litellm_config.yaml:ro
    depends_on:
      litellm-db:
        condition: service_healthy
      litellm-redis:
        condition: service_started
    restart: unless-stopped
    networks:
      - devcontainer-network

  litellm-redis:
    image: redis:7-alpine
    container_name: litellm-redis
    ports:
      - "6380:6379"
    volumes:
      - litellm-redis-data:/data
    networks:
      - devcontainer-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # n8n Services
  n8n-postgres:
    image: postgres:16-alpine
    container_name: n8n-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=n8n
      - POSTGRES_PASSWORD=${N8N_POSTGRES_PASSWORD:-n8n_password}
      - POSTGRES_DB=n8n
    volumes:
      - n8n-postgres-data:/var/lib/postgresql/data
    networks:
      - devcontainer-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U n8n"]
      interval: 10s
      timeout: 5s
      retries: 5

  n8n-redis:
    image: redis:7-alpine
    container_name: n8n-redis
    restart: unless-stopped
    networks:
      - devcontainer-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  n8n-server:
    image: n8nio/n8n:1.101.1
    container_name: n8n-server
    restart: unless-stopped
    depends_on:
      n8n-postgres:
        condition: service_healthy
      n8n-redis:
        condition: service_healthy
    ports:
      - "5678:5678"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=false
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://localhost:5679/
      - GENERIC_TIMEZONE=UTC
      - N8N_LOG_LEVEL=info
      - N8N_GRACEFUL_SHUTDOWN_TIMEOUT=30
      - N8N_HIRING_BANNER_ENABLED=false
      - N8N_LOG_OUTPUT=console
      - N8N_VERSION_NOTIFICATIONS_ENABLED=false
      - NODE_ENV=production
      - NODE_FUNCTION_ALLOW_BUILTIN=*
      - NODE_FUNCTION_ALLOW_EXTERNAL=*
      - OFFLOAD_MANUAL_EXECUTIONS_TO_WORKERS=true
      - N8N_PUBLIC_API_DISABLED=false
      - N8N_PUBLIC_API_ENDPOINT=api
      - N8N_PUBLIC_API_SWAGGERUI_DISABLED=false
      - N8N_RUNNERS_ENABLED=true
      - N8N_RUNNERS_HEARTBEAT_INTERVAL=30
      - N8N_RUNNERS_MAX_CONCURRENCY=20
      - N8N_RUNNERS_MODE=internal
      - N8N_RUNNERS_TASK_TIMEOUT=60
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=n8n-postgres
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=n8n
      - DB_POSTGRESDB_USER=n8n
      - DB_POSTGRESDB_PASSWORD=${N8N_POSTGRES_PASSWORD:-n8n_password}
      - EXECUTIONS_MODE=queue
      - QUEUE_BULL_REDIS_HOST=n8n-redis
      - QUEUE_BULL_REDIS_PORT=6379
      - QUEUE_HEALTH_CHECK_ACTIVE=true
      - N8N_METRICS=true
      - N8N_METRICS_INCLUDE_DEFAULT_METRICS=true
      - N8N_METRICS_INCLUDE_API_ENDPOINTS=true
      - N8N_METRICS_INCLUDE_WORKFLOW_ID_LABEL=true
      - N8N_DISABLE_UI=false
    volumes:
      - n8n-data:/home/node/.n8n
    networks:
      - devcontainer-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5678/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  n8n-webhook:
    image: n8nio/n8n:1.101.1
    container_name: n8n-webhook
    restart: unless-stopped
    depends_on:
      n8n-postgres:
        condition: service_healthy
      n8n-redis:
        condition: service_healthy
    ports:
      - "5679:5678"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: webhook
    environment:
      - N8N_BASIC_AUTH_ACTIVE=false
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://localhost:5679/
      - GENERIC_TIMEZONE=UTC
      - N8N_LOG_LEVEL=info
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=n8n-postgres
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=n8n
      - DB_POSTGRESDB_USER=n8n
      - DB_POSTGRESDB_PASSWORD=${N8N_POSTGRES_PASSWORD:-n8n_password}
      - EXECUTIONS_MODE=queue
      - QUEUE_BULL_REDIS_HOST=n8n-redis
      - QUEUE_BULL_REDIS_PORT=6379
      - QUEUE_HEALTH_CHECK_ACTIVE=true
      - N8N_METRICS=true
      - N8N_METRICS_INCLUDE_DEFAULT_METRICS=true
    volumes:
      - n8n-data:/home/node/.n8n
    networks:
      - devcontainer-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5678/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  n8n-worker:
    image: n8nio/n8n:1.101.1
    container_name: n8n-worker
    restart: unless-stopped
    depends_on:
      n8n-postgres:
        condition: service_healthy
      n8n-redis:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: worker
    environment:
      - N8N_BASIC_AUTH_ACTIVE=false
      - GENERIC_TIMEZONE=UTC
      - N8N_LOG_LEVEL=info
      - N8N_CONCURRENCY_PRODUCTION_LIMIT=20
      - N8N_DEFAULT_BINARY_DATA_MODE=default
      - N8N_DIAGNOSTICS_ENABLED=false
      - N8N_DISABLE_PRODUCTION_MAIN_PROCESS=true
      - N8N_DISABLE_UI=true
      - N8N_GRACEFUL_SHUTDOWN_TIMEOUT=30
      - N8N_LOG_OUTPUT=console
      - N8N_VERSION_NOTIFICATIONS_ENABLED=false
      - NODE_ENV=production
      - NODE_FUNCTION_ALLOW_BUILTIN=*
      - NODE_FUNCTION_ALLOW_EXTERNAL=*
      - OFFLOAD_MANUAL_EXECUTIONS_TO_WORKERS=true
      - N8N_PUBLIC_API_DISABLED=true
      - N8N_PUBLIC_API_ENDPOINT=api
      - N8N_PUBLIC_API_SWAGGERUI_DISABLED=false
      - N8N_RUNNERS_ENABLED=true
      - N8N_RUNNERS_HEARTBEAT_INTERVAL=30
      - N8N_RUNNERS_MAX_CONCURRENCY=20
      - N8N_RUNNERS_MODE=internal
      - N8N_RUNNERS_TASK_TIMEOUT=60
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=n8n-postgres
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=n8n
      - DB_POSTGRESDB_USER=n8n
      - DB_POSTGRESDB_PASSWORD=${N8N_POSTGRES_PASSWORD:-n8n_password}
      - EXECUTIONS_MODE=queue
      - QUEUE_BULL_REDIS_HOST=n8n-redis
      - QUEUE_BULL_REDIS_PORT=6379
      - QUEUE_HEALTH_CHECK_ACTIVE=true
      - N8N_METRICS=true
      - N8N_METRICS_INCLUDE_DEFAULT_METRICS=true
    volumes:
      - n8n-data:/home/node/.n8n
    networks:
      - devcontainer-network

  # SigNoz Services
  signoz-init-clickhouse:
    image: clickhouse/clickhouse-server:25.5.6
    container_name: signoz-init-clickhouse
    command:
      - bash
      - -c
      - |
        cd /tmp
        tar -xvzf histogram-quantile.tar.gz
        mkdir -p /var/lib/clickhouse/user_scripts
        mv histogram-quantile /var/lib/clickhouse/user_scripts/histogramQuantile
        ls /var/lib/clickhouse/user_scripts
    restart: on-failure
    volumes:
      - signoz-clickhouse:/var/lib/clickhouse/
      - ./configs/signoz/histogram-quantile.tar.gz:/tmp/histogram-quantile.tar.gz
    networks:
      - devcontainer-network

  signoz-zookeeper-1:
    image: signoz/zookeeper:3.7.1
    container_name: signoz-zookeeper-1
    user: root
    restart: unless-stopped
    volumes:
      - signoz-zookeeper-1:/bitnami/zookeeper
    environment:
      - ZOO_SERVER_ID=1
      - ALLOW_ANONYMOUS_LOGIN=yes
      - ZOO_AUTOPURGE_INTERVAL=1
      - ZOO_ENABLE_PROMETHEUS_METRICS=yes
      - ZOO_PROMETHEUS_METRICS_PORT_NUMBER=9141
    healthcheck:
      test:
        - CMD-SHELL
        - echo srvr | nc -w 3 localhost 2181 2>/dev/null | grep -q "Zookeeper version" && exit 0 || exit 1
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 30s
    networks:
      - devcontainer-network

  signoz-clickhouse:
    image: clickhouse/clickhouse-server:25.5.6
    container_name: signoz-clickhouse
    tty: true
    restart: unless-stopped
    depends_on:
      signoz-init-clickhouse:
        condition: service_completed_successfully
      signoz-zookeeper-1:
        condition: service_healthy
    volumes:
      - ./configs/signoz/clickhouse/config.xml:/etc/clickhouse-server/config.xml
      - ./configs/signoz/clickhouse/users.xml:/etc/clickhouse-server/users.xml
      - ./configs/signoz/clickhouse/custom-function.xml:/etc/clickhouse-server/custom-function.xml
      - ./configs/signoz/clickhouse/cluster.xml:/etc/clickhouse-server/config.d/cluster.xml
      - signoz-clickhouse:/var/lib/clickhouse/
    environment:
      - CLICKHOUSE_SKIP_USER_SETUP=1
    healthcheck:
      test:
        - CMD
        - wget
        - --spider
        - -q
        - 0.0.0.0:8123/ping
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s
    ulimits:
      nproc: 65535
      nofile:
        soft: 262144
        hard: 262144
    networks:
      - devcontainer-network

  signoz-schema-migrator-sync:
    image: signoz/signoz-schema-migrator:${OTELCOL_TAG:-v0.129.7}
    container_name: schema-migrator-sync
    command:
      - sync
      - --dsn=tcp://signoz-clickhouse:9000
      - --up=
    depends_on:
      signoz-zookeeper-1:
        condition: service_healthy
      signoz-clickhouse:
        condition: service_healthy
    restart: on-failure
    networks:
      - devcontainer-network

  signoz:
    image: signoz/signoz:${SIGNOZ_VERSION:-v0.97.0}
    container_name: signoz
    command:
      - --config=/root/config/prometheus.yml
    ports:
      - "3301:8080" # SigNoz UI (changed from 8080 to avoid conflict with Open WebUI)
    volumes:
      - ./configs/signoz/signoz/prometheus.yml:/root/config/prometheus.yml
      - ./configs/signoz/dashboards:/root/config/dashboards
      - signoz-sqlite:/var/lib/signoz/
    environment:
      - SIGNOZ_ALERTMANAGER_PROVIDER=signoz
      - SIGNOZ_TELEMETRYSTORE_CLICKHOUSE_DSN=tcp://signoz-clickhouse:9000
      - SIGNOZ_SQLSTORE_SQLITE_PATH=/var/lib/signoz/signoz.db
      - DASHBOARDS_PATH=/root/config/dashboards
      - STORAGE=clickhouse
      - GODEBUG=netdns=go
      - TELEMETRY_ENABLED=true
      - DEPLOYMENT_TYPE=docker-standalone-amd
      - DOT_METRICS_ENABLED=true
    depends_on:
      signoz-clickhouse:
        condition: service_healthy
      signoz-schema-migrator-sync:
        condition: service_completed_successfully
    healthcheck:
      test:
        - CMD
        - wget
        - --spider
        - -q
        - localhost:8080/api/v1/health
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    networks:
      - devcontainer-network

  signoz-otel-collector:
    image: signoz/signoz-otel-collector:${OTELCOL_TAG:-v0.129.7}
    container_name: signoz-otel-collector
    command:
      - --config=/etc/otel-collector-config.yaml
      - --manager-config=/etc/manager-config.yaml
      - --copy-path=/var/tmp/collector-config.yaml
      - --feature-gates=-pkg.translator.prometheus.NormalizeName
    volumes:
      - ./configs/signoz/otel-collector-config.yaml:/etc/otel-collector-config.yaml
      - ./configs/signoz/signoz/otel-collector-opamp-config.yaml:/etc/manager-config.yaml
    environment:
      - OTEL_RESOURCE_ATTRIBUTES=host.name=signoz-host,os.type=linux
      - LOW_CARDINAL_EXCEPTION_GROUPING=false
    ports:
      - "4317:4317" # OTLP gRPC receiver
      - "4318:4318" # OTLP HTTP receiver
    depends_on:
      signoz-clickhouse:
        condition: service_healthy
      signoz:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - devcontainer-network

  signoz-schema-migrator-async:
    image: signoz/signoz-schema-migrator:${OTELCOL_TAG:-v0.129.7}
    container_name: schema-migrator-async
    command:
      - async
      - --dsn=tcp://signoz-clickhouse:9000
      - --up=
    depends_on:
      signoz-clickhouse:
        condition: service_healthy
      signoz-schema-migrator-sync:
        condition: service_completed_successfully
    restart: on-failure
    networks:
      - devcontainer-network

volumes:
  dify-db-data:
  dify-redis-data:
  dify-storage:
  open-webui-data:
  litellm-data:
  litellm-db-data:
  litellm-redis-data:
  n8n-data:
  n8n-postgres-data:
  signoz-clickhouse:
  signoz-sqlite:
  signoz-zookeeper-1:

networks:
  devcontainer-network:
    driver: bridge
