# TODO

- [ ] init workflow
  - user can create new task or get workflow context from `Open WebUI`
  - user can integrate external services using `dify` and `n8n`

- [ ] use script create skip the setup steps
  - [ ] litellm setup:
    - create admin user
      username: `admin`
      password: `sk-nftgo-1234-abcd`
    - add deepseek api key
    - add openrouter api key
  - [ ] signoz setup:
    - create signoz admin user
      email: `admin@example.com`
      password: `Admin_example123`

  - [ ] open webui setup:
    - create open webui admin user
      username: `example`
      email: `example@example.com`
      password: `examplepassword`
    - import configs

  - [ ] integration
    - create litellm apikey
    - update apikey on open webui

  - [ ] create dify admin user
