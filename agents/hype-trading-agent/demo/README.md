```bash
.
├── 0_0_1 # version 0.0.1 of the demo
│   ├── data-source-schema.md # schema for data sources used in the demo, saved in ./temp-data/*.json
│   ├── main.py # main script to run the version 0.0.1 demo
│   ├── plan.md # description of the demo plan
│   └── temp-data # temporary data storage, for example trades, candles, etc.
├── address-info-collect.py
├── README.md
└── scripts # helper scripts for the demo
```
